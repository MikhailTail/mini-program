# AI 闯关答题小程序 · 功能与使用说明

> 把企业内部资料一键变成考核题：员工在微信里答题，管理者看团队薄弱点。
> 支持「粘贴资料直接出题」和「企业知识库精准出题」两种方式，AI 全程参与出题、判分、复盘。

---

## 一、功能介绍

小程序围绕一条核心链路：**输入内容 → AI 生成题目 → 用户答题 → AI 生成分析报告**，共 4 个页面。

### 1. AI 出题（首页）

- **粘贴资料出题**：把公司制度、培训笔记、产品资料等文本直接粘贴到输入框（最多 10 万字），AI 自动生成题目
- **文档上传出题**：支持上传 **doc / docx / pdf / txt / md / wps** 文档，以及 **jpg / png 等图片和扫描件**（自动 OCR 识别文字），解析后自动填入资料框
- **自定义出题数量**：可设置 1 道以上任意数量的题目，默认 5 道
- **题型多样**：自动混合生成单选、多选、判断、填空、简答等题型

### 2. 企业知识库（RAG 精准出题）

- **文档入库**：上传 PDF / MD / Word 等文档到知识库，AI 自动分块、向量化、建索引
- **检索出题（A 模式）**：输入出题要求，AI 只依据**库内资料**出题，不掺入通用知识
- **知识问答（B 模式）**：对库内资料直接提问，AI 只按库内文档回答，并附上**资料出处引用**
- **企业码隔离**：通过企业邀请码区分不同组织的知识库，数据互不可见

### 3. 答题闯关

- 逐题作答，进度以星级显示，支持单选 / 多选 / 判断 / 填空 / 简答五种题型
- 多选可勾选多个选项；填空、简答直接输入文字
- 提交后自动判分，逐题记录对错

### 4. AI 分析报告

- 展示得分（百分制）、答对题数、评级（优秀 / 良好 / 及格 / 挂科）
- AI 生成**总体评价**、**薄弱知识点**、**学习建议**三部分复盘
- 答题记录与报告可留存至云数据库（云开发），便于后续团队侧统计

### 5. 其他特性

| 特性 | 说明 |
|------|------|
| 无登录 | 设备 ID + 临时企业码即可使用，免注册 |
| 数据隔离 | `corp_code` 企业码贯穿全链路，组织间互不可见 |
| 多端 | 一套代码编译到微信小程序 + H5 双端 |
| 极简 UI | 手绘"草稿纸"风格，答题轻量无压力 |

---

## 二、使用说明

### 方式一：粘贴资料出题（快速上手）

1. 打开小程序，首页点击 **「＋ 粘贴资料 / 上传文档」**
2. 把制度、培训笔记、产品资料等文本**粘贴到资料框**（至少 10 个字）
3. 设置**出题数量**（如 5），可填**企业邀请码**（可选，用于数据隔离）
4. 点击 **「开始闯关 →」**，等待 AI 出题
5. 逐题作答 → 最后一题点击 **「提交并看报告 🎉」**
6. 查看成绩单，点击 **「生成 AI 复盘报告 →」** 获得薄弱点与学习建议

### 方式二：上传文档出题

1. 首页点击 **「＋ 粘贴资料 / 上传文档」** 进入资料页
2. 点击 **「＋ 上传文档 / 图片 / 扫描件」**，选择文件
   - 文字文档（doc / pdf / txt 等）自动解析为文本
   - 图片 / 扫描件自动 OCR 识别
3. 解析成功后资料自动载入，后续步骤与方式一相同

### 方式三：企业知识库精准出题 / 问答

1. 首页点击 **「＋ 企业知识库（RAG 精准出题）」**
2. 填写**企业邀请码**（默认 `default`，不同组织用不同码隔离知识库）
3. 点击 **「＋ 上传文档入库」**，选择 PDF / MD / Word 等文档，等待向量化入库
4. 选择模式：
   - **A · 检索出题**：设置出题数量，在输入框写清出题要求（如"根据库里的考勤制度出 5 道题"），点击 **「按资料出题 →」**
   - **B · 知识问答**：在输入框直接提问（如"我们公司的带薪年假怎么算？"），点击 **「检索回答 →」**，会展示答案及资料出处
5. 出题后自动进入答题闯关 → 报告流程

### 常见说明

- **出题数量留空**：提交时会提示"出题数量至少 1 题"，失焦后留空会自动恢复为 5
- **资料少于 10 字**：无法出题，会提示补充资料
- **知识库回答带"资料依据"**：说明答案来自库内文档，可点击展开查看原文出处

---

## 三、项目结构

```
mini-program/
├── backend/                 # FastAPI 后端
│   ├── app/
│   │   ├── api/v1/routes/   # 路由层： quiz / answer / report / upload / rag
│   │   ├── llm/             # LangChain 链： 出题 / 判分 / 报告 / 问答
│   │   ├── rag/             # RAG： chunker / embedding / store / service
│   │   ├── models/          # 数据模型（题库 / 答题 / 报告）
│   │   ├── services/        # 服务层
│   │   ├── utils/           # 文档解析 / OCR / ID 生成
│   │   └── main.py          # 入口（同源托管 H5 构建产物）
│   ├── rag_data/            # Chroma 向量库数据
│   ├── requirements.txt     # 后端依赖
│   ├── run_server.bat       # 一键启动脚本
│   └── install_rag.bat      # RAG 依赖安装（注意顺序）
├── frontend/                # Taro 前端
│   └── src/
│       ├── pages/           # index（首页） / kb（知识库） / quiz（答题） / report（报告）
│       ├── api/             # 接口封装（相对路径，dev 走 /api 代理）
│       └── config/          # Taro 编译配置（含 devServer 代理）
├── prototypes/              # 交互原型（HTML）
├── README.md                # 开发向说明
├── 小程序.md                # 本文档（功能 + 使用说明）
└── 安装说明.md               # 安装部署说明
```

## 四、主要接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/quiz/generate` | 文本生成题库 |
| POST | `/api/v1/quiz/submit` | 提交答案并判分 |
| POST | `/api/v1/quiz/upload-doc` | 上传文档转文本 |
| POST | `/api/v1/quiz/upload-image` | 上传图片 OCR |
| POST | `/api/v1/report/generate` | 生成分析报告 |
| POST | `/api/v1/rag/index` | 文档建库（离线） |
| POST | `/api/v1/rag/ask` | 检索出题 / 知识问答 |
| GET  | `/health` | 健康检查 |

---

## 五、技术栈与运行

**技术栈**：Taro 4（React）· FastAPI · LangChain · DeepSeek · Chroma 向量库 · bge-small-zh 本地向量化

完整安装步骤（Python / Node / 依赖 / 环境变量 / 启动方式）见 **[《安装说明.md》](安装说明.md)**。

```bash
# 1. 后端：安装依赖并启动（端口 8000）
cd backend
pip install -r requirements.txt        # 基础依赖
install_rag.bat                         # RAG 依赖（注意安装顺序）
# 配置 backend/.env 填入 DEEPSEEK_API_KEY 后：
run_server.bat

# 2. 前端：安装依赖并启动 H5 开发（端口 10086，已配置 /api 代理）
cd frontend
npm install
npm run dev:h5
```

访问 H5：`http://localhost:10086`；生产部署时将根目录 `dist` 构建产物由后端同源托管。微信小程序端使用微信开发者工具导入**项目根目录** `f:/mini-program` 即可（构建产物与云函数均位于根目录 `dist/`、`cloudfunctions/`）。
