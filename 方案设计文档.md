# 《AI 闯关答题小程序（企业内部版）》方案设计文档

> 版本：v1.0　|　日期：2026-08-26　|　作者：独立开发者（产品 + 开发）
> 定位：**企业内部答题 / 培训考核小程序**（在 liyupi/yu-ai-learn 同技术栈基础上做差异化）
> 关联文档：《需求分析文档 v1.0》

---

## 0. 文档导读与差异化定位

### 0.1 参照基线

本项目**完全参照**已验证可行的开源项目 `liyupi/yu-ai-learn`（2026-08，技术栈 Taro + FastAPI + LangChain + DeepSeek）。其已具备：

- 后端分层：API 路由 → Service → Repository → Model，LLM 走 LangChain 工厂 + 结构化输出
- 核心链路：`quiz`（出题）/ `report`（分析报告）/ `knowledge`（RAG 知识库）/ `user`（用户系统）
- 前端：Taro 小程序，页面 `index / knowledge / quiz / report / profile`
- 已含原型图（`prototypes/`），证明"输入→出题→答题→报告"闭环可行

### 0.2 我们的差异化（企业内部分）

| 维度 | 参照项目（个人学习） | 本项目（企业内部） |
|------|----------------------|--------------------|
| 用户模型 | 个人账号体系（user 表） | **无登录 / 设备+部门标识**，企业内免注册 |
| 内容来源 | 个人上传 + RAG 通用知识库 | **企业私有资料库**（制度/产品/SOP 转考题） |
| 核心场景 | 个人闯关刷分 | **培训考核 + 团队排行榜 + 正确率分布** |
| 报告对象 | 个人复盘 | **个人 + 管理者视角的团队薄弱点报告** |
| 社交 | 个人分享 | **部门挑战 / 内部排行榜**（非公开裂变） |
| 部署 | 个人云 | 可**企业私有化 / 内网**部署（数据安全） |

> 一句话：把"个人 AI 闯关刷题"改造成"企业把内部资料一键变成考核题，员工在微信里答题、管理者看团队薄弱点"。这是需求分析文档中 **C 端企业培训** 主线的落地形态。

### 0.3 MVP 第一步约束（方案 B，已确认）

- **不做登录注册、不做账号体系**（满足"先跑通核心流程"要求）
- 后端 **FastAPI + 轻量 SQLite**（非 MySQL），按「设备 ID / 临时企业码」区分数据，零外部依赖
- 优先跑通：**用户输入内容 → AI 生成题目 → 用户答题 → AI 生成分析报告**
- 数据库仅承载"本次生成的题库 + 答题结果 + 报告"，不做用户持久化 profile

---

## 1. 小程序技术选型重点对比

> 你要求**重点对比小程序技术选型**。以下为 2026 年主流方案的五维评估。

### 1.1 候选方案

| 方案 | 说明 |
|------|------|
| **微信原生**（WXML/WXSS/JS） | 官方原生开发 |
| **Taro**（React/Vue 写，编译到小程序） | 京东开源，跨端编译 |
| **uni-app**（Vue 写，编译到多端） | DCloud 出品 |
| **微信云开发**（原生 + 云函数 + 云数据库） | 官方 Serverless |

### 1.2 五维对比表

| 维度 | 微信原生 | Taro | uni-app | 微信云开发 |
|------|----------|------|---------|------------|
| **性能** | ★★★★☆ 最优 | ★★★★ 接近原生 | ★★★☆ 略逊 | ★★★ 云函数有冷启延迟 |
| **AI / 后端接入灵活度** | ★★★ 需自建后端 | ★★★★★ 直接调 FastAPI | ★★★★ 需自建或云函数 | ★★ 强绑云数据库，DeepSeek 需走云函数 |
| **多端扩展** | ★ 仅微信 | ★★★★★ 微信/H5/RN/支付宝 | ★★★★★ 全端最强 | ★ 仅微信 |
| **团队匹配度（React 栈）** | ★★ 需学特有语法 | ★★★★★ 复用 React 生态 | ★★★ Vue 栈更优 | ★★ |
| **开发/调试体验** | ★★★ 官方工具 | ★★★★ VSCode + HMR | ★★★★ 自带 IDE | ★★★ 云函数调试弱 |
| **审核/风险** | 标准 | 标准 | 标准 | 标准（云函数也需审核） |
| **AI 生成内容合规** | 需自建 | 后端 FastAPI 统一过滤 | 后端统一过滤 | 云函数内过滤 |

### 1.3 结论：为什么选 Taro

1. **AI 后端自由**：本项目核心是把 DeepSeek/LangChain 放在自有 FastAPI 后端，Taro 作为纯前端调用 HTTP 接口，不受云开发数据库绑定限制 → 契合方案 B。
2. **React 生态复用**：个人开发者用 React 栈，组件、状态管理、Context7 文档齐全，维护成本低。
3. **多端预留**：未来企业可能要 H5 内嵌 OA / 企业微信网页，Taro 一键扩。
4. **参照项目即 Taro**：直接复用 `yu-ai-learn` 的前端结构，降低试错成本。
5. 微信原生虽性能最优，但 AI 接入需自己写请求层、无 React 复用；云开发强绑腾讯云、DeepSeek 走云函数受限；uni-app 偏 Vue，与团队栈不符。

> **最终选型：Taro（React）+ FastAPI + LangChain + DeepSeek + 轻量 SQLite**，与需求分析文档一致，且被参照项目验证。

---

## 2. 总体架构

### 2.1 分层架构（沿用参照项目，适配企业无登录）

```
┌─────────────────────────────────────────────┐
│  微信小程序 (Taro/React)                       │
│  pages: index(首页/企业资料) · quiz(答题)      │
│         report(分析报告) · rank(部门榜)        │
└───────────────┬─────────────────────────────┘
                │ HTTPS (FastAPI, CORS 放开小程序域名)
┌───────────────▼─────────────────────────────┐
│  FastAPI 后端                                  │
│  api/v1/routes:  quiz · report · knowledge     │
│  services:      quiz_service · report_service  │
│                  knowledge_service(企业资料)    │
│  llm:  langchain_factory(DeepSeek) ·           │
│        quiz_chain · report_chain(结构化输出)    │
│  repositories:  quiz_repo · report_repo        │
│  db:   SQLite (题库/答题/报告，按企业码隔离)     │
└───────────────┬─────────────────────────────┘
                │ OpenAI-compatible API
        ┌───────▼────────┐
        │  DeepSeek API   │  (中文强、成本低)
        └────────────────┘
```

### 2.2 与企业版的差异点落地

- **企业资料库（knowledge）**：管理员/HR 上传制度 PDF → 存企业码隔离的文档表 → 出题时从该文档上下文生成（RAG-lite，MVP 可先全文拼接，非必上向量库）。
- **部门标识**：小程序启动时传入「企业邀请码」，后端据此隔离数据，无需登录。
- **团队报告**：`report` 接口除个人报告外，提供聚合接口（管理者视图：部门正确率、薄弱知识点）。MVP 先做个人报告，聚合为二期。

---

## 3. 核心业务流程（MVP 第一步，方案 B）

> 目标：零登录、零账号，跑通 **输入 → 出题 → 答题 → 报告**。

### 3.1 流程时序

```
用户(小程序)               FastAPI                 DeepSeek/LangChain
   │                          │                          │
   │ 1.输入内容(文本/粘贴/企业文档ID)                    │
   │─────────────────────────►│                          │
   │                          │ 2.构造 prompt + schema    │
   │                          │─────────────────────────►│
   │                          │ 3.结构化出题(JSON)        │
   │                          │◄─────────────────────────│
   │ 4.返回题库(存SQLite, 本次) │                          │
   │◄─────────────────────────│                          │
   │ 5.逐题作答                                            
   │ 6.提交答案                │                          │
   │─────────────────────────►│                          │
   │                          │ 7.判分 + 薄弱点归纳        │
   │                          │─────────────────────────►│
   │                          │ 8.分析报告(JSON)          │
   │                          │◄─────────────────────────│
   │ 9.展示报告(存SQLite)      │                          │
   │◄─────────────────────────│                          │
```

### 3.2 模块职责（对应参照项目文件，改造点标注）

| 模块 | 参照项目文件 | 本项目改造 |
|------|--------------|------------|
| 出题 | `llm/quiz_chain.py` + `prompts/quiz_prompt.py` | prompt 加入"企业考核语气/考点"约束 |
| 报告 | `llm/report_chain.py` + `prompts/report_prompt.py` | 报告含"团队建议"字段（二期用） |
| 结构化 | `llm/output_schemas.py` | 复用 Pydantic schema |
| 路由 | `api/v1/routes/quiz.py`, `report.py` | 去掉 user 依赖，改企业码 |
| 存储 | `repositories/quiz_repository.py` | SQLite 替代 MySQL |
| 前端页 | `pages/quiz`, `pages/report` | 加"企业资料入口""部门榜" |

---

## 4. 数据库设计（轻量 SQLite，方案 B）

> 不做 user 表。仅承载本次业务数据，按 `corp_code` 逻辑隔离。

```sql
-- 企业/部门标识（轻量，非账号）
CREATE TABLE corp (
  code        TEXT PRIMARY KEY,   -- 企业邀请码
  name        TEXT,
  created_at  DATETIME
);

-- 一次生成的题库任务
CREATE TABLE quiz_task (
  id          TEXT PRIMARY KEY,
  corp_code   TEXT,
  source_type TEXT,               -- text / doc
  source_ref  TEXT,               -- 文本摘要 或 文档ID
  created_at  DATETIME
);

-- 题目
CREATE TABLE question (
  id          TEXT PRIMARY KEY,
  task_id     TEXT,
  q_type      TEXT,               -- single / judge / blank / short
  content     TEXT,
  options     TEXT,               -- JSON
  answer      TEXT,
  analysis    TEXT,               -- AI 解析
  point       TEXT                -- 考点标签
);

-- 答题结果（本次）
CREATE TABLE answer_record (
  id          TEXT PRIMARY KEY,
  task_id     TEXT,
  question_id TEXT,
  selected    TEXT,
  correct     INTEGER,
  device_id   TEXT                -- 设备标识，替代登录
);

-- 分析报告
CREATE TABLE report (
  id          TEXT PRIMARY KEY,
  task_id     TEXT,
  corp_code   TEXT,
  score       INTEGER,
  weak_points TEXT,              -- JSON 薄弱点
  suggestion  TEXT,
  created_at  DATETIME
);
```

> 注：`corp` 表在 MVP 可简化为前端传入 `corp_code` 字符串，后端不强制预注册，降低第一步复杂度。

---

## 5. 关键技术实现要点

### 5.1 LangChain 接入 DeepSeek（结构化出题）

参照项目用 `langchain_factory.py` 通过 OpenAI 兼容协议接入模型，`with_structured_output(PydanticSchema)` 强制题型 JSON。本项目同样实现，prompt 改造为企业考核风格：

```python
# llm/langchain_factory.py（示意，沿用参照）
from langchain_openai import ChatOpenAI
def build_chat(model="deepseek-chat"):
    return ChatOpenAI(
        api_key=settings.DEEPSEEK_API_KEY,
        base_url="https://api.deepseek.com/v1",
        model=model, temperature=0.3
    )
```

```python
# 出题链路
chain = build_chat().with_structured_output(QuizSchema)
quiz = chain.invoke(quiz_prompt.format(content=source, n=10))
```

### 5.2 报告生成（分析报告链路）

`report_chain` 输入"题目 + 用户作答"，输出分数、薄弱知识点、学习建议。MVP 用一次 LLM 调用完成，成本低。

### 5.3 前端 Taro 调用

`services/api.ts` 封装 `POST /api/v1/quiz/generate`、`POST /api/v1/report/generate`，小程序 `pages/quiz` 轮播答题，`pages/report` 渲染报告。参照项目已有完整页面，直接改造。

### 5.4 企业资料接入（二期增强）

MVP 第一步支持"文本粘贴/手动输入"即可跑通；企业 PDF 上传走 `knowledge` 模块（参照项目已含 `document_loader_service` + `vector_store_service`），二期开启 RAG 精准出题。

### 5.5 合规与成本

- 小程序 AI 内容需合规：FastAPI 在返回前做敏感词过滤（参照项目 `core/security.py` 可复用思路）。
- DeepSeek 成本：MVP 免费期，按需求分析文档估算月成本数百元级。
- 生成时延：出题 5–15s，前端 loading + 异步任务反馈。

---

## 6. 开发流程与分步实现计划

> 你要求**分步骤、先核心后扩展**。以下为执行顺序。

### 阶段 0：工程脚手架（复用参照项目）
- 拉取 `yu-ai-learn` 结构，删除 user/登录相关代码
- 后端改 SQLite（`scripts/init_mysql.py` → `init_sqlite.py`）
- 前端 Taro 工程保留 `index/quiz/report`，加 `rank`

### 阶段 1（MVP 核心闭环，最高优先）✅ 先做的
1. **输入**：小程序首页支持文本粘贴 / 选企业资料（MVP 仅文本）
2. **AI 出题**：`POST /quiz/generate` → DeepSeek 出 10 题（选择/判断/简答）
3. **答题**：`pages/quiz` 逐题作答，即时判分
4. **报告**：`POST /report/generate` → 分数 + 薄弱点 + 建议
5. **存储**：SQLite 落库（task/question/answer/report），无登录

> 此阶段**不做**：登录注册、用户系统、RAG 向量库、部门榜、企业后台。

### 阶段 2（企业差异化增强）
- 企业 PDF/文档上传 → `knowledge` 模块 → RAG 出题
- 部门标识 + 团队正确率聚合报告（管理者视图）
- 部门排行榜 `pages/rank`

### 阶段 3（留存与社交，呼应需求分析亮点）
- 错题本 + 间隔重复（微信订阅消息推送）
- "挑战卡"分享到企业群（内部裂变，非公开）
- 职场模板库（产品/编程/制度考点）

### 阶段 4（变现，需求分析第 6 章）
- 企业版 SaaS：按企业席位/题库量收费
- 私有化部署选项（数据安全卖点）

---

## 7. 其他方案设计重点

### 7.1 接口设计（MVP 核心）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/quiz/generate` | 入参 `{corp_code, content, n}` → 返回题目列表 + task_id |
| POST | `/api/v1/quiz/submit` | 入参 `{task_id, answers[], device_id}` → 判分 |
| POST | `/api/v1/report/generate` | 入参 `{task_id}` → 分析报告 |
| GET  | `/api/v1/report/{task_id}` | 取历史报告（本次会话） |

### 7.2 测试策略
- 复用参照项目 `tests/`：出题 schema 契约测试 `test_prompt_contract.py`、判分 `test_scoring.py`
- 新增：无登录隔离测试（corp_code 数据不串）

### 7.3 部署
- 后端：Docker（参照 `backend/Dockerfile`），或轻量服务器直接 `uvicorn`
- 前端：`taro build --type weapp` → 微信开发者工具上传
- 企业私有化：后端可内网部署，前端配内网域名

### 7.4 风险与对策
| 风险 | 对策 |
|------|------|
| 小程序审核（AI 类目） | 提前备《算法备案》思路，内容合规过滤 |
| DeepSeek 不稳定 | 后端做超时重试 + 降级提示 |
| 无登录导致数据易丢 | MVP 明确"本次会话留存"，二期上企业账号 |
| 企业资料泄密 | 二期私有化部署 + 文档不出企业内网 |

---

## 8. 结论

- **技术选型可靠**：Taro + FastAPI + LangChain + DeepSeek + SQLite 被参照项目 `yu-ai-learn` 完整验证，且完美契合"AI 后端自由 + React 复用 + 多端预留"需求。
- **差异化清晰**：从个人刷题转向"企业内部资料→考核题→团队报告"，填补企业微信生态内 AI 考核空白（需求分析 C 主线）。
- **分步可行**：阶段 1 零登录跑通核心闭环，个人开发者可低成本立即启动；企业增强与变现按阶段 2–4 迭代。

> 文档完。下一步可据此直接搭建工程脚手架（阶段 0 + 阶段 1）。
