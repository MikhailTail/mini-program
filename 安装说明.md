# 安装说明

> 适用项目：AI 闯关答题小程序
> 环境：Windows（本说明以 Windows / PowerShell 为例）

---

## 一、环境要求

| 组件 | 要求 | 说明 |
|------|------|------|
| 操作系统 | Windows 10 / 11 | 其他平台可参照执行 |
| Python | **3.10+** | 建议使用 Anaconda 独立环境 |
| Node.js | **18+** | Taro 4 要求 |
| npm | 9+ | 随 Node 安装 |
| 微信开发者工具 | 最新版（可选） | 仅编译运行小程序端时需要 |

---

## 二、后端安装（FastAPI）

### 1. 创建 / 进入 Python 环境

```powershell
conda create -n ai-quiz python=3.11 -y
conda activate ai-quiz
```



### 2. 安装基础依赖

```powershell
cd f:\mini-program\backend
pip install -r requirements.txt
```

`requirements.txt` 已固定版本，核心内容：

| 包 | 版本 | 用途 |
|------|------|------|
| fastapi | 0.115.0 | Web 框架 |
| uvicorn[standard] | 0.30.6 | ASGI 服务器 |
| pydantic | 2.9.2 | 数据校验 |
| pydantic-settings | 2.5.2 | 配置管理 |
| python-multipart | 0.0.12 | 文件上传解析 |
| python-dotenv | 1.0.1 | 环境变量读取 |
| httpx | 0.27.2 | HTTP 客户端 |
| langchain | 0.3.0 | LLM 编排框架 |
| langchain-openai | 0.2.0 | DeepSeek(OpenAI 兼容)接入 |
| pypdf | 5.1.0 | PDF 文本抽取 |
| olefile | 0.47 | doc/wps 老格式解析 |

### 3. 安装 OCR 依赖（图片 / 扫描件识别）

> **版本约束**：`opencv-contrib` 4.10 必须搭配 `numpy 1.x`（opencv 4.11 强制 numpy 2.x，与 paddle 2.6 冲突）。已写入 `requirements.txt`，直接随第 2 步安装即可。

| 包 | 版本 | 用途 |
|------|------|------|
| paddlepaddle | 2.6.2 | OCR 深度学习框架 |
| paddleocr | 2.9.1 | 文字识别引擎 |
| pymupdf | 1.24.10 | PDF 渲染 |
| numpy | 1.26.4 | 数值计算（锁定 1.x） |
| opencv-contrib-python | 4.10.0.84 | 图像处理 |

### 4. 安装 RAG 依赖（Chroma + 本地 embedding）

> **依赖冲突**：`chromadb` 与 `paddle 2.6.2` / `protobuf` 存在冲突，**必须按顺序安装**。直接执行脚本：

```powershell
cd f:\mini-program\backend
install_rag.bat your_python_exe
```

脚本内部顺序（等价手动命令）：

```powershell
pip install chromadb==0.5.23 sentence-transformers==3.3.1
pip install protobuf==3.20.3 posthog==3.5.0
```

| 包 | 版本 | 用途 / 说明 |
|------|------|------|
| chromadb | 0.5.23 | 向量数据库 |
| sentence-transformers | 3.3.1 | 本地 embedding（模型 `BAAI/bge-small-zh-v1.5`） |
| protobuf | 3.20.3 | **必须降级**，否则 paddle 2.6.2 崩溃 |
| posthog | 3.5.0 | **必须降级**，修复 chromadb 遥测刷错 |

> 安装失败时添加国内镜像：`pip install ... -i https://mirrors.aliyun.com/pypi/simple/`

### 5. 配置环境变量

复制 `backend/.env.example` 为 `backend/.env` 并填写：

```ini
# DeepSeek API 配置（https://platform.deepseek.com 申请）
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 服务配置
APP_HOST=0.0.0.0
APP_PORT=8000
CORS_ORIGINS=*
```

> 首次使用 RAG 会从 HuggingFace 下载 `bge-small-zh-v1.5` 模型（约 100MB），国内网络可通过 `RAG_EMBEDDING_CACHE_DIR` 指定缓存目录或配置镜像。

### 6. 启动后端

```powershell
cd f:\mini-program\backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

或双击 `run_server.bat`。验证：

```powershell
curl http://127.0.0.1:8000/health
# {"status":"ok","model":"deepseek-chat"}
```

> 注意：如启用了 `--reload`，首次改动代码后需重启使 RAG 路由生效。

---

## 三、前端安装（Taro 4）

### 1. 安装依赖

```powershell
cd f:\mini-program\frontend
npm install
```

### 2. 启动 H5 开发模式（推荐调试方式）

```powershell
npm run dev:h5
```

- 默认端口 **10086**：`http://localhost:10086`
- `config/index.js` 已配置 devServer 代理：`/api → http://127.0.0.1:8000`，前端请求可写相对路径
- **注意**：修改代理配置后需重启 dev server 生效

### 3. 启动微信小程序开发模式

```powershell
npm run dev:weapp
```

- 生成 `dist/` 后用微信开发者工具导入 `frontend/dist` 目录
- 小程序端需在开发者工具中配置「不校验合法域名」或后端使用 HTTPS 域名

### 4. 生产构建

```powershell
npm run build:h5    # H5 生产构建
npm run build:weapp # 小程序生产构建
```

生产 H5 由 FastAPI 同源托管（`app/main.py` 自动挂载 `frontend/dist`），同源部署无需额外配置跨域。

---

## 四、技术栈与版本清单

### 后端技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| Web 框架 | FastAPI 0.115.0 + Uvicorn 0.30.6 | 异步 API 服务 |
| 数据校验 | Pydantic 2.9.2 | 请求 / 响应模型 |
| 数据库 | SQLite | 题库 / 答题 / 报告落库 |
| LLM 编排 | LangChain 0.3.0 | 出题 / 判分 / 报告 / 问答链 |
| 大模型 | DeepSeek `deepseek-chat` | OpenAI 兼容接口 |
| 向量检索 | Chroma 0.5.23 | RAG 知识库存储 |
| Embedding | `BAAI/bge-small-zh-v1.5`（本地） | 离线向量化（DeepSeek 无 embedding 接口） |
| OCR | PaddleOCR 2.9.1 + PaddlePaddle 2.6.2 | 图片 / 扫描件文字识别旁路 |
| 文档解析 | pypdf / olefile / pymupdf | doc / docx / pdf / txt / md / wps 转文本 |

### 前端技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 跨端框架 | Taro | **4.2.1**（`@tarojs/*` 全系） |
| 视图框架 | React / React DOM | **18.2.0** |
| 编译目标 | 微信小程序 + H5 | webpack 5 runner |
| 语言 | JavaScript（JSX） | — |
| 样式 | CSS / Sass | — |
| 构建配置 | babel-preset-taro 4.2.1 | Babel 7 |
| 开发辅助 | react-refresh-webpack-plugin 0.6.2 + react-refresh 0.14.2 | HMR 热更新 |
| 测试辅助（可选） | puppeteer-core 25.9.0 | UI 自动化调试 |

### 前端依赖版本表（package.json）

```jsonc
// dependencies（生产）
"@tarojs/*": "4.2.1",          // components / taro / runtime / react / helper / shared
"@tarojs/plugin-framework-react": "4.2.1",
"@tarojs/plugin-platform-h5": "4.2.1",
"@tarojs/plugin-platform-weapp": "4.2.1",
"react": "^18.2.0",
"react-dom": "^18.2.0",
"@babel/runtime": "^7.21.0",

// devDependencies（开发）
"@tarojs/cli": "4.2.1",
"@tarojs/webpack5-runner": "4.2.1",
"babel-preset-taro": "4.2.1",
"@babel/core": "^7.24.0",
"@babel/preset-react": "^7.24.0",
"@pmmmwh/react-refresh-webpack-plugin": "^0.6.2",
"react-refresh": "^0.14.2",
"puppeteer-core": "^25.9.0"    // 可选
```

---

## 五、常见问题

| 问题 | 处理 |
|------|------|
| `pip install` 慢 / 失败 | 加国内镜像 `-i https://mirrors.aliyun.com/pypi/simple/` |
| paddle 启动崩溃（protobuf 报错） | 确认 `protobuf==3.20.3`（见 RAG 安装步骤） |
| chromadb 持续刷遥测日志 | 确认 `posthog==3.5.0` |
| 前端请求 404 | dev 模式需后端运行在 8000 且 devServer 代理已生效（改代理后重启） |
| 前端请求跨域报错 | dev 用代理；生产由后端同源托管 dist，无需跨域 |
| 小程序 request 失败 | 开发者工具勾选「不校验合法域名」 |
| `rag/ask` / `rag/index` 405 | 后端未加载新路由，重启 uvicorn（勿用旧进程） |
| RAG 建库慢 | 首次需下载 bge 模型；建库为离线任务，日志观察进度 |
| 端口冲突 | 后端 8000、前端 10086 被占用时改 `run_server.bat` / `config/index.js` |
